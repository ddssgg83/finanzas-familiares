const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  // Si en algún momento quieres ignorar errores de TS en build:
  // typescript: { ignoreBuildErrors: true },
};

export default nextConfig;
